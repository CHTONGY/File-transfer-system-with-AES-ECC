plugins {
    id 'java'
}

group 'FileTransferSystemWithECCAndAES'
version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
    compile(project(':lib'))
    compile(project(':foo'))
    compile(files('javax.json-1.1.2.jar'))
    compile(files('javax.json-api-1.1.2.jar'))
}
